{
  plugins = {
    comment = {
      enable = true;
      settings = {
        padding = true;
      };
    };

    markdown-preview.enable = true;

    harpoon = {
      enable = true;
    };
  };
}
